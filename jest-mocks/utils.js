// Mock utils for testing

// Mock createRipple function
function createRipple(event, element) {
  // Mock implementation - do nothing in tests
  return;
}

// Mock applyTheme function
function applyTheme(theme) {
  // Mock implementation - do nothing in tests
  return;
}

module.exports = {
  createRipple,
  applyTheme,
};
